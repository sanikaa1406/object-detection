# object-detection
A real-time object detection system built using **YOLOv4-Tiny**, **OpenCV**, and **Python GUI (Tkinter)**.   The application detects objects through a live camera feed and triggers alerts when specific objects (like a person) are detected.  
